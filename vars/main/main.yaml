---
httpd_service: "{{ lookup('vars', ansible_os_family+'_service') }}"

httpd_pkgs: "{{ lookup('vars', ansible_os_family+'_pkgs') }}"

httpd_config: "{{ lookup('vars', ansible_os_family+'_config') }}"
